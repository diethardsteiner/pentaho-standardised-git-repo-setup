#######################################################
###      PROPERTIES FOR ${PSGRS_PROJECT_NAME}             ###
#######################################################

# Project name
PROP_PROJECT_NAME=${PSGRS_PROJECT_NAME}
# Environment
PROP_ENVIRONMENT=${PSGRS_ENV}
# Path to folder that holds the git repos
PROP_BASE_DIR=${PSGRS_BASE_DIR}
# Project properties home
PROP_PROJECT_PROPERTIES_HOME=${PSGRS_BASE_DIR}/${PSGRS_PROJECT_NAME}-config-${PSGRS_ENV}
# Path to SQL Folder
PROP_SQL_FOLDER_PATH=${PSGRS_BASE_DIR}/${PSGRS_PROJECT_NAME}-code/pdi/sql
# Path to the modules folder within the PDI repo
PROP_PDI_MODULES_REPO_PATH=modules