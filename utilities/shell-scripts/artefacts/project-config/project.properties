#######################################################
###      PROPERTIES FOR ${PROJECT_NAME}             ###
#######################################################

# Project name
PARAM_PROJECT_NAME=${PROJECT_NAME}
# Environment
PARAM_ENVIRONMENT=${PDI_ENV}
# Path to folder that holds the git repos
PARAM_BASE_DIR=${BASE_DIR}
# Project properties home
PARAM_PROJECT_PROPERTIES_HOME=${PARAM_BASE_DIR}/${PROJECT_NAME}-config-${PDI_ENV}
# Path to SQL Folder
PARAM_SQL_FOLDER_PATH=
# Path to the modules folder within the PDI repo
PARAM_PDI_MODULES_REPO_PATH=
